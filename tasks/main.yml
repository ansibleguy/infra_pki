---

- name: ROLE | Checking config
  ansible.builtin.assert:
    that:
      - app is defined

- name: ROLE | Processing debian config
  ansible.builtin.import_tasks: debian/main.yml
  when: "ansible_distribution|lower in ['debian', 'ubuntu']"
